//JSX = HTML(predefine tags) in  JS =====> extended HTML===>creating userdefine tag (we can create tag like <App></App>)

function App() {    
  return    <h1>"Hello World"</h1>;
}

export default App;